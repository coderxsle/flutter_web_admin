import 'package:flutter/material.dart';

class UserRolesPage extends StatelessWidget {
  const UserRolesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('用户角色', style: TextStyle(fontSize: 24)),
    );
  }
}
