import 'package:flutter/material.dart';

class UserMenuPermissionsPage extends StatelessWidget {
  const UserMenuPermissionsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('用户菜单权限', style: TextStyle(fontSize: 24)),
    );
  }
}
