import 'package:flutter/material.dart';

class UserRolesPermissionsPage extends StatelessWidget {
  const UserRolesPermissionsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('用户角色权限', style: TextStyle(fontSize: 24)),
    );
  }
}
