---
globs: *.dart
---

# Dart 代码换行与风格约定

本项目中 Dart 代码（尤其是后端 `Endpoint` 等业务类）在换行和排版上，应尽量遵循紧凑、少换行的风格，整体可参考  
`flutter_web_server/lib/src/endpoints/auth_endpoint copy.dart`。

- **总体原则：尽量单行，避免“过度换行”**
  - 能在一行内清晰表达的语句，**优先保持一行**，不要机械地把每个参数或每个调用都拆成多行。
  - 仅当一行明显过长（例如超过约 100–120 列）或可读性明显下降时，再考虑换行。

- **函数声明与参数列表**
  - 函数/方法声明（返回类型 + 名称 + 参数）如果长度适中，**保持在一行**：

```dart
Future<CommonResponse> login(Session session, String username, String password) async {
```

  - 只有在参数极多、单行明显过长时，才将参数拆行；拆行时尽量保持紧凑，而不是每个参数一行。

- **方法调用与链式调用**
  - 普通方法调用（包括带 `where` 闭包的查询）如果能够放在一行且可读，**保持单行**：

```dart
var user = await SysUser.db.findFirstRow(session, where: (t) => t.username.equals(username) & t.deleted.equals(false));
```

  - 链式调用或嵌套调用时，也应先尝试保持在一行；只有在内容非常长导致阅读困难时，才将调用拆成多行。
  - 拆行时，尽量按语义块（如：不同阶段的查询、不同层级的调用）进行分行，而不要把每个参数孤立地放在单独一行。

- **控制流与逻辑表达式**
  - `if` 条件表达式、三元表达式等，只要逻辑清晰，**优先使用单行**。
  - 复杂布尔表达式（多个 `&&` / `||`）在阅读困难时，可以按逻辑分组适度换行，但避免“每个条件一行”的极端拆分。

- **注释与文档注释**
  - 使用紧凑的文档注释风格，例如：

```dart
/// 后台登录接口
Future<CommonResponse> login(...) async { ... }
```

  - 行内注释尽量放在代码上一行或同一行尾部，不要为了注释刻意增加大量空行。

- **格式化风格的取舍**
  - 编写 Dart 代码时，**不要完全照搬 Flutter 默认格式化所产生的“每个参数一行”风格**。
  - 在保证基本可读性的前提下，优先保持语句和参数紧凑在同一行，通过减少不必要的换行来提升整体阅读流畅度。
  - 如需在“自动格式化结果”和上述紧凑风格之间做选择，**以本规则和 `auth_endpoint copy.dart` 的现有风格为主**。

